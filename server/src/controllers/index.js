const userController = require("./user");
const postController = require("./posting");
const likeController = require("./like");

module.exports = {
  userController,
  postController,
  likeController,
};
